// src/app/models/search-result.model.ts
export interface SearchResult {
  id: string;                     // Unique identifier for the recipe
  title: string;                  // Title of the recipe
  image: string;                  // URL of the recipe image
}
